(library
  (name drumtools)
  (modules Configuration Partition)
  (libraries base stdio))

(executable
  (name PngCroper)
  (modules PngCroper)
  (libraries drumtools camlimages.png))

(executable
  (name PartitionGenerator)
  (modules PartitionGenerator)
  (libraries str drumtools))
  
