(library
  (name drumtools)
  (modules Configuration Partition)
  (libraries base stdio))

(executable
  (name PngCroper)
  (modules PngCroper)
  (libraries camlimages.png))

(executable
  (name PartitionGenerator)
  (modules Configuration PartitionGenerator)
  (libraries str))
  
